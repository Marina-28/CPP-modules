#include "ScavTrap.hpp"

int main()
{
    // ClapTrap a;

    // a.attack("Bob");
    // a.takeDamage(9);
    // a.beRepaired(5);
    // a.getInfo();

    ScavTrap b;

    b.attack("Misha");
    b.takeDamage(10);
    b.beRepaired(5);
    b.getInfo();
}